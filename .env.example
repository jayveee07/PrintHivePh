# Optional local override for the customer chat endpoint.
# Use a free external backend such as Cloudflare Workers if you stay on Firebase Spark.
VITE_AI_CHAT_ENDPOINT="https://your-worker-name.your-account.workers.dev"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="https://printhiveph-20at26.web.app/"
